{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "affinity": {
      "required": [],
      "title": "affinity",
      "type": "object"
    },
    "extraArgs": {
      "description": "Extra CLI args appended to the controller container.",
      "items": {
        "required": []
      },
      "title": "extraArgs",
      "type": "array"
    },
    "extraEnv": {
      "description": "Extra environment variables for the controller container.",
      "items": {
        "required": []
      },
      "title": "extraEnv",
      "type": "array"
    },
    "extraVolumeMounts": {
      "description": "Extra volume mounts on the controller container (pairs with extraVolumes).",
      "items": {
        "required": []
      },
      "title": "extraVolumeMounts",
      "type": "array"
    },
    "extraVolumes": {
      "description": "Extra volumes on the controller pod. Use this to make a filesystem-backend\nrepository reachable in-process (hostPath/NFS/PVC) so the controller can run\nshort idempotent kopia ops directly. The e2e harness uses a hostPath here.",
      "items": {
        "required": []
      },
      "title": "extraVolumes",
      "type": "array"
    },
    "features": {
      "description": "=============================================================================\nFeature permissions\n=============================================================================\nA few opt-in features need the operator to WRITE Secrets in the namespaces it\nmanages, so each grants extra RBAC. All are OFF by default (least privilege):\nthe chart does not hand out cluster-wide `secrets` write until you turn on the\nfeature you use. The flag names match the CRD fields that trigger them.\n\nSymptom → fix: if you enable the feature in a CR but forget the flag here, the\nresource's `.status` shows an actionable condition like\n  \"... (HTTP 403). ... Fix: set features.\u003cname\u003e.enabled: true ...\"\nSet the matching flag below and the operator heals on the next reconcile.\nSee docs/feature-permissions.md for the full mapping and security trade-offs.",
      "properties": {
        "credentialProjection": {
          "properties": {
            "enabled": {
              "default": false,
              "description": "Grant the operator `secrets` create+patch+delete so\n`spec.credentialProjection` works: the operator copies a repository's\ncredential Secret(s) into each mover Job's namespace (the chief win is a shared\nClusterRepository whose Secret is pinned to one namespace) and cleans its\ncopies up again (legacy-copy sweep, reap-on-shrink, reap-on-disable).\nSECURITY TRADE-OFF: `create` cannot be scoped to a Secret name, so the\noperator can write a Secret in any namespace it manages.",
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "credentialProjection",
          "type": "object"
        },
        "kopiaUi": {
          "properties": {
            "enabled": {
              "default": false,
              "description": "Grant the operator `secrets` create+patch+delete so `spec.server` (the\nkopia web-UI) works: it creates a generated-auth Secret, mirrors each of a\nClusterRepository's credential Secrets into the server namespace, and deletes\nall of them on teardown. SECURITY TRADE-OFF: grants unscoped create/delete on\nSecrets in any namespace the operator manages.",
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "kopiaUi",
          "type": "object"
        }
      },
      "required": [],
      "title": "features",
      "type": "object"
    },
    "fullnameOverride": {
      "default": "",
      "description": "Override the full release-qualified name (defaults to \"\u003crelease\u003e-kopiur\").",
      "title": "fullnameOverride",
      "type": "string"
    },
    "global": {
      "description": "=============================================================================\nGlobal (chart-wide fallbacks + umbrella-injectable subset)\n=============================================================================\nkopiur has no subcharts, so `global` does not auto-propagate anywhere on its\nown. Its two jobs: (a) kopiur's own templates read `global.*` as a shared\nfallback for the controller (root) and the webhook, so you set a fleet-wide\ndefault once instead of repeating it per component; (b) if kopiur is ever\nwrapped in an umbrella chart, Helm merges the umbrella's `global` down and\nkopiur inherits these for free. Merge rule: list-valued globals\n(imagePullSecrets, tolerations, topologySpreadConstraints) are CONCATENATED\nwith the component value; map-valued globals (nodeSelector, affinity) are used\nonly when the component leaves its own value empty; commonLabels are added to\nevery rendered object.",
      "properties": {
        "affinity": {
          "required": [],
          "title": "affinity",
          "type": "object"
        },
        "commonLabels": {
          "description": "Labels stamped on every rendered object (fleet-wide labelling).",
          "required": [],
          "title": "commonLabels",
          "type": "object"
        },
        "imagePullSecrets": {
          "description": "Concatenated with the top-level imagePullSecrets; reaches controller,\nwebhook, and mover Jobs.",
          "items": {
            "required": []
          },
          "title": "imagePullSecrets",
          "type": "array"
        },
        "nodeSelector": {
          "description": "Scheduling defaults every kopiur pod inherits unless the component\n(root = controller, webhook.*) sets its own.",
          "required": [],
          "title": "nodeSelector",
          "type": "object"
        },
        "tolerations": {
          "items": {
            "required": []
          },
          "title": "tolerations",
          "type": "array"
        },
        "topologySpreadConstraints": {
          "items": {
            "required": []
          },
          "title": "topologySpreadConstraints",
          "type": "array"
        }
      },
      "required": [],
      "title": "global",
      "type": "object"
    },
    "image": {
      "description": "=============================================================================\nController image\n=============================================================================\nThree images ship with kopiur:\n  * controller — the operator (reconcilers); exposes /metrics. Configured here.\n  * webhook    — the axum admission server; see webhook.image below.\n  * mover      — the per-Job image (one Job per Backup/Restore/Maintenance\n                 run); see mover.image below.\nWhen `digest` is set it wins over `tag` (tag defaults to .Chart.AppVersion).\nCharts published to oci://ghcr.io/home-operations/charts ship all three\ndigests pinned to the release images (the in-repo defaults stay empty and\nfloat on tags); on a published chart set digest: \"\" to make tag effective.",
      "properties": {
        "digest": {
          "default": "",
          "description": "Pin by digest (e.g. \"sha256:...\"); takes precedence over tag.",
          "title": "digest",
          "type": "string"
        },
        "pullPolicy": {
          "default": "IfNotPresent",
          "description": "Image pull policy for the controller.",
          "title": "pullPolicy",
          "type": "string"
        },
        "repository": {
          "default": "ghcr.io/home-operations/kopiur-controller",
          "description": "Full controller image repository (registry + path).",
          "title": "repository",
          "type": "string"
        },
        "tag": {
          "default": "",
          "description": "Defaults to .Chart.AppVersion when empty.",
          "title": "tag",
          "type": "string"
        }
      },
      "required": [],
      "title": "image",
      "type": "object"
    },
    "imagePullSecrets": {
      "description": "Image pull secrets for the controller pod (concatenated with\nglobal.imagePullSecrets; also reaches the webhook pod and mover Jobs).",
      "items": {
        "required": []
      },
      "title": "imagePullSecrets",
      "type": "array"
    },
    "installScope": {
      "default": "cluster",
      "description": "=============================================================================\nInstall scope\n=============================================================================\n\"cluster\" (default) or \"namespaced\".\n  cluster    — RBAC is a ClusterRole/ClusterRoleBinding. The operator manages\n    kopiur.home-operations.com resources cluster-wide AND reconciles\n    ClusterRepository. This is the default because a namespace-scoped Role\n    silently disables the ClusterRepository kind (a cluster-scoped resource is\n    out of a Role's reach), turning kopiur into a shared-backup-tier operator\n    that can't do shared backup tiers.\n  namespaced — RBAC is a namespace-scoped Role/RoleBinding: the operator\n    manages resources only in its own namespace and ClusterRepository is NOT\n    reconciled. Choose this as the explicit least-privilege opt-down for a\n    single-team install where the reduced blast radius is worth losing\n    cluster-scoped repositories.",
      "title": "installScope",
      "type": "string"
    },
    "leaderElection": {
      "description": "Enable leader election. Required when replicaCount \u003e 1; harmless at 1.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "enabled",
          "type": "boolean"
        },
        "flowSchema": {
          "description": "API Priority and Fairness lane for the election Lease. Kubernetes' own\n`system-leader-election` FlowSchema covers only kube-system ServiceAccounts,\nso an operator in its own namespace has its lease renewals queued behind all\nother ServiceAccount traffic in `workload-low` — the congestion behind the\ncontroller restart storms in #319. This puts Kopiur's `leases` calls (and\nonly those) into the built-in guaranteed `leader-election` priority level.\nTurn off if APF is disabled on the cluster or you cannot create\ncluster-scoped flowcontrol.apiserver.k8s.io objects.",
          "properties": {
            "enabled": {
              "default": true,
              "title": "enabled",
              "type": "boolean"
            },
            "matchingPrecedence": {
              "default": 200,
              "description": "Ordering against other FlowSchemas; lower wins. Must stay above the\nbuilt-in `system-leader-election` (100) so kube's own controllers keep\npriority, and below `service-accounts` (9000) or this has no effect.",
              "title": "matchingPrecedence",
              "type": "integer"
            }
          },
          "required": [],
          "title": "flowSchema",
          "type": "object"
        },
        "timings": {
          "description": "Leader-election protocol timings, in seconds. Leave unset to use the\nclient-go defaults (lease 15 / renewDeadline 10 / renewPeriod 2 / retry 2).\n`renewDeadline` is a budget for a whole ROUND of renew attempts, not one\nattempt's timeout, and `renewPeriod + renewDeadline` must stay strictly\nunder `leaseDuration` — the operator rejects any other combination at\nstartup rather than risk two replicas both believing they lead. Widen these\nonly if your control plane genuinely cannot answer a lease write inside the\ndefault window; prefer leaving `flowSchema` enabled instead.",
          "required": [],
          "title": "timings",
          "type": "object"
        }
      },
      "required": [],
      "title": "leaderElection",
      "type": "object"
    },
    "livenessProbe": {
      "description": "Liveness probe for the controller container. The whole probe is passed\nthrough with `toYaml`, so you can retune timings/thresholds or swap the scheme\nentirely; set to `{}` to drop the probe. `port: metrics` is the named\ncontainer port (metrics.port above).",
      "properties": {
        "httpGet": {
          "properties": {
            "path": {
              "default": "/healthz",
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "metrics",
              "title": "port",
              "type": "string"
            }
          },
          "required": [],
          "title": "httpGet",
          "type": "object"
        },
        "initialDelaySeconds": {
          "default": 10,
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 15,
          "title": "periodSeconds",
          "type": "integer"
        }
      },
      "required": [],
      "title": "livenessProbe",
      "type": "object"
    },
    "logging": {
      "description": "=============================================================================\nLogging (stdout / `kubectl logs`, always on)\n=============================================================================\nControls the fmt logging that every component writes to stdout — the stream\n`kubectl logs` shows. The controller passes RUST_LOG + KOPIUR_LOG_FORMAT\nthrough to mover Jobs, so a mover honors the same level and format. This is a\nchart-wide concern (all three components honor it), which is why it stays a\nnamed top-level block rather than folding under the controller. Env var names\nmatch crates/telemetry/src/env.rs. (For pushing logs/traces to a collector,\nsee observability.otlp below — a different, optional path.)",
      "properties": {
        "format": {
          "default": "text",
          "description": "Console format: \"text\" (human-readable, default) or \"json\" (one structured\nobject per line for Loki/ELK/Datadog). Unknown values degrade to text.",
          "title": "format",
          "type": "string"
        },
        "level": {
          "default": "info",
          "description": "Log level / filter directive (RUST_LOG style: error|warn|info|debug|trace;\nper-target works too, e.g. \"info,kopia=debug\" to see kopia's own progress in\nmover logs).",
          "title": "level",
          "type": "string"
        }
      },
      "required": [],
      "title": "logging",
      "type": "object"
    },
    "maxConcurrentDeleteJobs": {
      "default": 0,
      "description": "0 = uncapped (default); set to bound concurrent snapshot-delete batch\nJobs across repositories (mass-deletion protection). Batching itself is the\nprimary protection — one Job per repository per accumulation window, not\none per Snapshot — so this cap is an opt-in backstop for a\nresource-constrained cluster, not a load-bearing safety mechanism: a small\ncap risks head-of-line-blocking every OTHER repository's deletions behind\none slow/failing one. It does NOT bound how many Snapshots one batch Job\ndeletes, nor does it gate whether a deletion is allowed at all — that's\ndeletionProtection.threshold on the Repository/ClusterRepository.",
      "minimum": 0,
      "title": "maxConcurrentDeleteJobs",
      "type": "integer"
    },
    "maxConcurrentJobs": {
      "default": 0,
      "description": "0 = uncapped (default); set to bound how many POOLED mover Jobs — backup\nsnapshots, restores, and the source side of either replication — may run at\nonce across ALL repositories. This is the cluster-operator BACKSTOP beneath\neach repository's own spec.concurrency.maxConcurrentJobs, which stays the\nprimary knob: a run must satisfy both, and whichever cap it meets first\nholds it. Set this when the node pool cannot host N movers no matter which\nrepositories they belong to; set the per-repository field when one BACKEND\nis the thing that must not be saturated. Restores are ALWAYS admitted and\nnever queued (a recovery in progress must not wait behind routine backups),\nthough a running restore still occupies a slot and so displaces backups.\nMaintenance, verification, pin and snapshot-delete Jobs are outside the pool\nentirely. Leaving this at 0 costs nothing: with no cap set anywhere the\noperator performs no extra API calls at all.",
      "minimum": 0,
      "title": "maxConcurrentJobs",
      "type": "integer"
    },
    "metrics": {
      "description": "=============================================================================\nController port \u0026 probes\n=============================================================================",
      "properties": {
        "port": {
          "default": 8081,
          "description": "The controller's single operational port. Rendered into KOPIUR_HTTP_ADDR\nas \"[::]:\u003cport\u003e\" (dual-stack wildcard), co-hosting /metrics, /healthz and\n/readyz. The metrics Service and the probes both target this port.",
          "title": "port",
          "type": "integer"
        }
      },
      "required": [],
      "title": "metrics",
      "type": "object"
    },
    "monitoring": {
      "description": "=============================================================================\nMonitoring (Prometheus + Grafana)\n=============================================================================\nThe controller-scrape ServiceMonitor, the alerting PrometheusRule, and the\nGrafana dashboard(s), grouped under one umbrella. NOTE: the webhook's own\nServiceMonitor is NOT here — it lives at webhook.serviceMonitor because it is\nan HTTPS scrape of the webhook's TLS port, a materially different scrape config\nthat moves with the webhook component, not this umbrella. The split is\ndeliberate.",
      "properties": {
        "dashboards": {
          "description": "Grafana dashboard(s) for the kopiur fleet. The same JSON lives in\ndeploy/dashboards/kopiur.json (the single source of truth, copied into the\nchart by `cargo xtask gen-all`) for manual import. By default it ships as a\nConfigMap labeled for the Grafana sidecar to auto-discover; flip\ngrafanaOperator.enabled to render a grafana-operator GrafanaDashboard CR from\nthe very same JSON instead.",
          "properties": {
            "annotations": {
              "description": "Extra annotations added to the dashboard object (ConfigMap or CR).",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "enabled": {
              "default": false,
              "description": "Create the dashboard (a sidecar ConfigMap by default).",
              "title": "enabled",
              "type": "boolean"
            },
            "folder": {
              "default": "",
              "title": "folder",
              "type": "string"
            },
            "folderAnnotation": {
              "default": "",
              "description": "Annotation setting the Grafana folder for the sidecar ConfigMap (optional).",
              "title": "folderAnnotation",
              "type": "string"
            },
            "grafanaOperator": {
              "properties": {
                "allowCrossNamespaceImport": {
                  "default": true,
                  "description": "Allow a Grafana in any namespace to import this GrafanaDashboard.",
                  "title": "allowCrossNamespaceImport",
                  "type": "boolean"
                },
                "enabled": {
                  "default": false,
                  "description": "Render a grafana-operator GrafanaDashboard CR instead of the sidecar ConfigMap.",
                  "title": "enabled",
                  "type": "boolean"
                },
                "folder": {
                  "default": "",
                  "description": "Folder to create the dashboard in (Grafana folder name).",
                  "title": "folder",
                  "type": "string"
                },
                "matchLabels": {
                  "description": "spec.instanceSelector.matchLabels — selects which Grafana instance(s) load this dashboard.",
                  "required": [],
                  "title": "matchLabels",
                  "type": "object"
                },
                "resyncPeriod": {
                  "default": "10m",
                  "description": "How often grafana-operator re-checks the dashboard for updates.",
                  "title": "resyncPeriod",
                  "type": "string"
                }
              },
              "required": [],
              "title": "grafanaOperator",
              "type": "object"
            },
            "label": {
              "default": "grafana_dashboard",
              "description": "Label the Grafana sidecar watches for (key: value). Adjust to your stack.",
              "title": "label",
              "type": "string"
            },
            "labelValue": {
              "default": "1",
              "title": "labelValue",
              "type": "string"
            },
            "labels": {
              "description": "Extra labels added to the dashboard object (ConfigMap or CR).",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "namespace": {
              "default": "",
              "description": "Namespace for the dashboard object; defaults to the release namespace.",
              "title": "namespace",
              "type": "string"
            }
          },
          "required": [],
          "title": "dashboards",
          "type": "object"
        },
        "prometheusRule": {
          "properties": {
            "backupStaleAfterSeconds": {
              "default": 172800,
              "description": "Age (seconds) after which a SnapshotPolicy's last success is \"stale\".",
              "title": "backupStaleAfterSeconds",
              "type": "integer"
            },
            "enabled": {
              "default": false,
              "description": "Create a Prometheus-Operator PrometheusRule with kopiur alerts.",
              "title": "enabled",
              "type": "boolean"
            },
            "labels": {
              "description": "Extra labels (e.g. to match your Prometheus ruleSelector).",
              "required": [],
              "title": "labels",
              "type": "object"
            }
          },
          "required": [],
          "title": "prometheusRule",
          "type": "object"
        },
        "serviceMonitor": {
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a Prometheus-Operator ServiceMonitor scraping the controller's\n/metrics (plain HTTP). Requires the ServiceMonitor CRD to exist.",
              "title": "enabled",
              "type": "boolean"
            },
            "interval": {
              "default": "30s",
              "description": "Scrape interval.",
              "title": "interval",
              "type": "string"
            },
            "labels": {
              "description": "Extra labels (e.g. to match your Prometheus serviceMonitorSelector).",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "metricRelabelings": {
              "items": {
                "required": []
              },
              "title": "metricRelabelings",
              "type": "array"
            },
            "relabelings": {
              "description": "Relabelings / metricRelabelings passed through verbatim.",
              "items": {
                "required": []
              },
              "title": "relabelings",
              "type": "array"
            },
            "scrapeTimeout": {
              "default": "10s",
              "description": "Scrape timeout.",
              "title": "scrapeTimeout",
              "type": "string"
            }
          },
          "required": [],
          "title": "serviceMonitor",
          "type": "object"
        }
      },
      "required": [],
      "title": "monitoring",
      "type": "object"
    },
    "mover": {
      "description": "=============================================================================\nMover (per-Job image)\n=============================================================================\nThe mover has no Deployment — the controller stamps one Job per\nBackup/Restore/Maintenance run, reading this image reference. Its security\ncontext and scheduling are controller-stamped, not chart values, so only the\nimage is configurable here.",
      "properties": {
        "image": {
          "properties": {
            "digest": {
              "default": "",
              "description": "Pin the mover image by digest so a re-pulled tag can never change what\nruns in a data-protection Job. STRONGLY RECOMMENDED in production.",
              "title": "digest",
              "type": "string"
            },
            "pullPolicy": {
              "default": "IfNotPresent",
              "description": "Pull policy used on the mover Job pods.",
              "title": "pullPolicy",
              "type": "string"
            },
            "repository": {
              "default": "ghcr.io/home-operations/kopiur-mover",
              "description": "Full mover image repository (registry + path).",
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "",
              "description": "Defaults to .Chart.AppVersion when empty.",
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image",
          "type": "object"
        }
      },
      "required": [],
      "title": "mover",
      "type": "object"
    },
    "nameOverride": {
      "default": "",
      "description": "Override the chart name used in resource names (defaults to .Chart.Name = \"kopiur\").",
      "title": "nameOverride",
      "type": "string"
    },
    "nodeSelector": {
      "description": "Scheduling controls (fall back to global.* when left empty).",
      "required": [],
      "title": "nodeSelector",
      "type": "object"
    },
    "observability": {
      "description": "=============================================================================\nOpenTelemetry (OTLP) — optional push of traces + logs (and a metrics push)\n=============================================================================\nSeparate from `logging` above (the always-on stdout stream): observability.otlp\nis the OPTIONAL push path. When enabled, the controller, webhook, and mover Jobs\nexport OTLP to the configured collector (the controller passes the same OTEL_*\nenv to movers). Metrics are ALSO always available via the Prometheus /metrics\npull endpoint; OTLP just adds a push path plus traces and logs. Env var names\nmatch crates/telemetry/src/env.rs.",
      "properties": {
        "otlp": {
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable OTLP export (sets OTEL_EXPORTER_OTLP_ENDPOINT on all components).",
              "title": "enabled",
              "type": "boolean"
            },
            "endpoint": {
              "default": "http://otel-collector.observability.svc:4317",
              "description": "Collector gRPC endpoint. Required when enabled. Only gRPC is compiled in.",
              "title": "endpoint",
              "type": "string"
            },
            "extraEnv": {
              "description": "Extra raw env (e.g. OTEL_TRACES_SAMPLER) added to every component.",
              "items": {
                "required": []
              },
              "title": "extraEnv",
              "type": "array"
            },
            "headers": {
              "default": "",
              "description": "OTEL_EXPORTER_OTLP_HEADERS, e.g. \"authorization=Bearer xyz\". Empty to omit.",
              "title": "headers",
              "type": "string"
            },
            "protocol": {
              "default": "grpc",
              "description": "OTEL_EXPORTER_OTLP_PROTOCOL (only \"grpc\" is supported by this build).",
              "title": "protocol",
              "type": "string"
            },
            "strict": {
              "default": false,
              "description": "Fail-fast on telemetry misconfiguration instead of degrading to fmt+pull.",
              "title": "strict",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "otlp",
          "type": "object"
        }
      },
      "required": [],
      "title": "observability",
      "type": "object"
    },
    "podAnnotations": {
      "required": [],
      "title": "podAnnotations",
      "type": "object"
    },
    "podDisruptionBudget": {
      "description": "PodDisruptionBudget for the controller. Keeps a voluntary disruption (node\ndrain, cluster upgrade) from taking the controller to zero replicas. Only\nuseful with replicaCount \u003e 1.",
      "properties": {
        "enabled": {
          "default": false,
          "title": "enabled",
          "type": "boolean"
        },
        "minAvailable": {
          "default": 1,
          "title": "minAvailable",
          "type": "integer"
        }
      },
      "required": [],
      "title": "podDisruptionBudget",
      "type": "object"
    },
    "podLabels": {
      "description": "Extra pod labels / annotations.",
      "required": [],
      "title": "podLabels",
      "type": "object"
    },
    "podSecurityContext": {
      "description": "=============================================================================\nController pod security (controller only)\n=============================================================================\nThe controller may need a RELAXED context — e.g. runAsUser: 1000 + fsGroup to\nread a filesystem/NFS-backed repository for in-process kopia ops (which is why\nextraVolumes exists on it). The webhook is pure admission and keeps its own\nlocked-down context under webhook.* so relaxing the controller never loosens\nthe webhook. Non-root uid 65534 (nobody) by default — distroless:nonroot.",
      "properties": {
        "fsGroup": {
          "default": 65534,
          "title": "fsGroup",
          "type": "integer"
        },
        "runAsGroup": {
          "default": 65534,
          "title": "runAsGroup",
          "type": "integer"
        },
        "runAsNonRoot": {
          "default": true,
          "title": "runAsNonRoot",
          "type": "boolean"
        },
        "runAsUser": {
          "default": 65534,
          "title": "runAsUser",
          "type": "integer"
        },
        "seccompProfile": {
          "properties": {
            "type": {
              "default": "RuntimeDefault",
              "title": "type",
              "type": "string"
            }
          },
          "required": [],
          "title": "seccompProfile",
          "type": "object"
        }
      },
      "required": [],
      "title": "podSecurityContext",
      "type": "object"
    },
    "priorityClassName": {
      "default": "",
      "description": "Pod-level priority class.",
      "title": "priorityClassName",
      "type": "string"
    },
    "rbac": {
      "description": "=============================================================================\nExtra RBAC\n=============================================================================",
      "properties": {
        "browseRole": {
          "default": false,
          "description": "Render an OPT-IN ClusterRole \"\u003crelease\u003e-browse\" carrying exactly what a\nhuman needs to run the `kubectl kopiur ls/cat/download/browse` data-plane:\nread snapshots/repositories, create/delete the session Job + ConfigMap, and\nexec into the session pod. It deliberately grants NO access to Secrets —\nthe session pod loads the repository credentials itself, so a browsing user\nnever reads them (`--local` is the exception and additionally needs `get\nsecrets`, granted separately). The chart only renders the ClusterRole; bind\nit to your users/groups with your own (Cluster)RoleBinding.",
          "title": "browseRole",
          "type": "boolean"
        }
      },
      "required": [],
      "title": "rbac",
      "type": "object"
    },
    "readinessProbe": {
      "description": "Readiness probe for the controller container (same passthrough as\nlivenessProbe; set to `{}` to drop it).",
      "properties": {
        "httpGet": {
          "properties": {
            "path": {
              "default": "/readyz",
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "metrics",
              "title": "port",
              "type": "string"
            }
          },
          "required": [],
          "title": "httpGet",
          "type": "object"
        },
        "initialDelaySeconds": {
          "default": 5,
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 10,
          "title": "periodSeconds",
          "type": "integer"
        }
      },
      "required": [],
      "title": "readinessProbe",
      "type": "object"
    },
    "reconcileConcurrency": {
      "default": 8,
      "description": "Per-controller cap on concurrently running reconciles (the operator runs\n8 controllers, so the process-wide worst case is 8x this). Bounds API-server\nload and file descriptors during re-list storms and API-server outages —\nunbounded reconcile concurrency is what let an apiserver flap exhaust the\ncontroller's fd table (EMFILE) within seconds. The default 8 clears a\nfew-hundred-object re-list in seconds while keeping slow reconciles (hooks,\nin-process kopia ops) from starving a controller. 0 = unbounded (the\npre-fix behavior; not recommended).",
      "maximum": 65535,
      "minimum": 0,
      "title": "reconcileConcurrency",
      "type": "integer"
    },
    "replicaCount": {
      "default": 1,
      "description": "=============================================================================\nController runtime behavior\n=============================================================================\nNumber of controller replicas. \u003e1 enables HA via leader election; only the\nelected leader reconciles, so deterministic jitter keeps schedules identical\nacross replicas and across failover. Pair \u003e1 with podDisruptionBudget and\ntopologySpreadConstraints below to make it genuinely highly-available.",
      "title": "replicaCount",
      "type": "integer"
    },
    "resources": {
      "description": "=============================================================================\nController workload \u0026 scheduling  (controller only — webhook has its own)\n=============================================================================\nResource requests for the controller pod.\nNo limits by default. No CPU limit (CPU throttling on an operator only adds\nreconcile latency; the request reserves a fair share). No memory limit either:\nthe controller's RSS can *burst* on startup/restart, not just steady state\n(~120Mi) — on (re)start it reconciles every existing resource at once, spawning\nconcurrent in-process `kopia` subprocesses (whose RSS counts against this\ncontainer's cgroup) to list/connect a repository that may hold many snapshots.\nA memory limit that doesn't cover that burst OOMKills the controller, which\nthen crash-loops (OOM -\u003e restart -\u003e re-reconcile burst -\u003e OOM). Set a limit\nonly if you've measured your own ceiling. See crates/e2e/tests/lifecycle.rs.",
      "properties": {
        "requests": {
          "properties": {
            "cpu": {
              "default": "50m",
              "title": "cpu",
              "type": "string"
            },
            "memory": {
              "default": "128Mi",
              "title": "memory",
              "type": "string"
            }
          },
          "required": [],
          "title": "requests",
          "type": "object"
        }
      },
      "required": [],
      "title": "resources",
      "type": "object"
    },
    "rwPublicationAdmission": {
      "description": "Mandatory admission protection for the explicitly opted-in Direct PVC\nRW-publication mode. This installs two cluster-scoped objects and therefore\nrequires cluster-admin installation rights even for namespaced operators.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Install the fail-closed RW-publication ValidatingAdmissionPolicy and Deny binding.\nRequired before any compatibility mover can run; the controller verifies the\ninstalled specs. Disabled by default to preserve existing installation scope.",
          "title": "enabled",
          "type": "boolean"
        }
      },
      "required": [],
      "title": "rwPublicationAdmission",
      "type": "object"
    },
    "securityContext": {
      "properties": {
        "allowPrivilegeEscalation": {
          "default": false,
          "title": "allowPrivilegeEscalation",
          "type": "boolean"
        },
        "capabilities": {
          "properties": {
            "drop": {
              "items": {
                "anyOf": [
                  {
                    "type": "string"
                  }
                ],
                "required": []
              },
              "title": "drop",
              "type": "array"
            }
          },
          "required": [],
          "title": "capabilities",
          "type": "object"
        },
        "readOnlyRootFilesystem": {
          "default": true,
          "title": "readOnlyRootFilesystem",
          "type": "boolean"
        }
      },
      "required": [],
      "title": "securityContext",
      "type": "object"
    },
    "serviceAccount": {
      "description": "=============================================================================\nServiceAccount\n=============================================================================",
      "properties": {
        "annotations": {
          "description": "Extra annotations (e.g. IRSA / Workload Identity role bindings).",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "automount": {
          "default": true,
          "description": "Mount the ServiceAccount token into the controller/webhook pods.",
          "title": "automount",
          "type": "boolean"
        },
        "create": {
          "default": true,
          "description": "Create the ServiceAccount. Disable to bring your own.",
          "title": "create",
          "type": "boolean"
        },
        "name": {
          "default": "",
          "description": "Name to use; defaults to the chart fullname when empty.",
          "title": "name",
          "type": "string"
        }
      },
      "required": [],
      "title": "serviceAccount",
      "type": "object"
    },
    "streamingLists": {
      "default": true,
      "description": "Use the Kubernetes WatchList streaming-list API for the controller's\ncluster-wide watches, lowering peak memory during the initial resync by\nstreaming pages instead of buffering them (the startup burst the resources\nnote below warns about). On by default: WatchList is GA in Kubernetes 1.34\n(beta 1.32/1.33) and the chart's kubeVersion floor is 1.32. Set `false` if\nyour apiserver has the WatchList feature gate disabled — the watches degrade\nto paged lists either way, but turning it off skips the feature probe.",
      "title": "streamingLists",
      "type": "boolean"
    },
    "tolerations": {
      "items": {
        "required": []
      },
      "title": "tolerations",
      "type": "array"
    },
    "topologySpreadConstraints": {
      "description": "Spread controller replicas across nodes/zones. Only meaningful with\nreplicaCount \u003e 1; pairs with podDisruptionBudget so a drain can't collapse\nboth replicas onto one node and then evict them together.",
      "items": {
        "required": []
      },
      "title": "topologySpreadConstraints",
      "type": "array"
    },
    "webhook": {
      "description": "=============================================================================\nAdmission webhook  (its own Deployment + Service + configs)\n=============================================================================\nA separate Deployment + Service, not folded into the controller. Everything\nthe webhook workload needs lives here (its own image, port, scheduling,\nsecurity context, PDB, spread, scrape) — a root-level key never touches it.",
      "properties": {
        "affinity": {
          "required": [],
          "title": "affinity",
          "type": "object"
        },
        "caBundle": {
          "default": "",
          "description": "Base64-encoded PEM CA bundle injected into the webhook configurations.\nOnly used when tls.mode is manual; required there so the API server trusts\nthe serving cert. Ignored in self and cert-manager modes (caBundle is\npopulated by the operator or cert-manager's ca-injector respectively).",
          "title": "caBundle",
          "type": "string"
        },
        "certManager": {
          "properties": {
            "issuerRef": {
              "description": "Use an existing Issuer/ClusterIssuer instead of the self-signed Issuer\nthis chart creates. Only used when tls.mode is cert-manager. Leave name\nempty to use the chart-managed self-signed Issuer.",
              "properties": {
                "kind": {
                  "default": "Issuer",
                  "title": "kind",
                  "type": "string"
                },
                "name": {
                  "default": "",
                  "title": "name",
                  "type": "string"
                }
              },
              "required": [],
              "title": "issuerRef",
              "type": "object"
            }
          },
          "required": [],
          "title": "certManager",
          "type": "object"
        },
        "enabled": {
          "default": true,
          "description": "Deploy the webhook (Deployment + Service + Validating/Mutating configs).\nWhen false, validation falls back to the controller's defensive checks only.",
          "title": "enabled",
          "type": "boolean"
        },
        "extraEnv": {
          "description": "Extra environment variables for the webhook container (list of\n`{name, value}` / `{name, valueFrom}` entries), appended after the\noperator-managed env. Mirrors the root `extraEnv`.",
          "items": {
            "required": []
          },
          "title": "extraEnv",
          "type": "array"
        },
        "failurePolicy": {
          "default": "Fail",
          "description": "failurePolicy for both webhook configurations: Fail (fail-closed,\nrecommended for a backup operator) or Ignore. Fail means a webhook outage\nblocks kopiur CR writes — see podDisruptionBudget below for why HA matters.",
          "title": "failurePolicy",
          "type": "string"
        },
        "image": {
          "description": "Admission webhook image (same tag/digest rules as the controller `image`).",
          "properties": {
            "digest": {
              "default": "",
              "description": "Pin by digest (e.g. \"sha256:...\"); takes precedence over tag.",
              "title": "digest",
              "type": "string"
            },
            "pullPolicy": {
              "default": "IfNotPresent",
              "description": "Image pull policy for the webhook.",
              "title": "pullPolicy",
              "type": "string"
            },
            "repository": {
              "default": "ghcr.io/home-operations/kopiur-webhook",
              "description": "Full webhook image repository (registry + path).",
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "",
              "description": "Defaults to .Chart.AppVersion when empty.",
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image",
          "type": "object"
        },
        "livenessProbe": {
          "description": "Liveness probe for the webhook container. Passed through with `toYaml`\n(retune timings/thresholds or swap the probe; `{}` drops it). The webhook\nonly serves HTTPS, so `scheme: HTTPS` on the named `https` port.",
          "properties": {
            "httpGet": {
              "properties": {
                "path": {
                  "default": "/healthz",
                  "title": "path",
                  "type": "string"
                },
                "port": {
                  "default": "https",
                  "title": "port",
                  "type": "string"
                },
                "scheme": {
                  "default": "HTTPS",
                  "title": "scheme",
                  "type": "string"
                }
              },
              "required": [],
              "title": "httpGet",
              "type": "object"
            },
            "initialDelaySeconds": {
              "default": 5,
              "title": "initialDelaySeconds",
              "type": "integer"
            },
            "periodSeconds": {
              "default": 15,
              "title": "periodSeconds",
              "type": "integer"
            }
          },
          "required": [],
          "title": "livenessProbe",
          "type": "object"
        },
        "nodeSelector": {
          "description": "Scheduling controls (fall back to global.* when left empty).",
          "required": [],
          "title": "nodeSelector",
          "type": "object"
        },
        "podAnnotations": {
          "required": [],
          "title": "podAnnotations",
          "type": "object"
        },
        "podDisruptionBudget": {
          "description": "PodDisruptionBudget for the webhook — the most important one to enable in\nHA. With failurePolicy: Fail, a node drain that evicts the only webhook\nreplica blocks every kopiur CR write until it reschedules; a PDB with\nreplicaCount \u003e 1 keeps one replica serving through the drain.",
          "properties": {
            "enabled": {
              "default": false,
              "title": "enabled",
              "type": "boolean"
            },
            "minAvailable": {
              "default": 1,
              "title": "minAvailable",
              "type": "integer"
            }
          },
          "required": [],
          "title": "podDisruptionBudget",
          "type": "object"
        },
        "podLabels": {
          "required": [],
          "title": "podLabels",
          "type": "object"
        },
        "podSecurityContext": {
          "description": "Pod security context for the webhook pod. Kept at the most locked-down\nposture (the webhook is pure admission and never touches a repository), so\nrelaxing the controller's context never loosens the webhook.",
          "properties": {
            "fsGroup": {
              "default": 65534,
              "title": "fsGroup",
              "type": "integer"
            },
            "runAsGroup": {
              "default": 65534,
              "title": "runAsGroup",
              "type": "integer"
            },
            "runAsNonRoot": {
              "default": true,
              "title": "runAsNonRoot",
              "type": "boolean"
            },
            "runAsUser": {
              "default": 65534,
              "title": "runAsUser",
              "type": "integer"
            },
            "seccompProfile": {
              "properties": {
                "type": {
                  "default": "RuntimeDefault",
                  "title": "type",
                  "type": "string"
                }
              },
              "required": [],
              "title": "seccompProfile",
              "type": "object"
            }
          },
          "required": [],
          "title": "podSecurityContext",
          "type": "object"
        },
        "port": {
          "default": 8443,
          "description": "The webhook's container port. Rendered into KOPIUR_WEBHOOK_ADDR as\n\"[::]:\u003cport\u003e\" (dual-stack wildcard); the Service maps 443 -\u003e this port.",
          "title": "port",
          "type": "integer"
        },
        "priorityClassName": {
          "default": "",
          "title": "priorityClassName",
          "type": "string"
        },
        "readinessProbe": {
          "description": "Readiness probe for the webhook container (same passthrough as\nlivenessProbe; set to `{}` to drop it).",
          "properties": {
            "httpGet": {
              "properties": {
                "path": {
                  "default": "/readyz",
                  "title": "path",
                  "type": "string"
                },
                "port": {
                  "default": "https",
                  "title": "port",
                  "type": "string"
                },
                "scheme": {
                  "default": "HTTPS",
                  "title": "scheme",
                  "type": "string"
                }
              },
              "required": [],
              "title": "httpGet",
              "type": "object"
            },
            "initialDelaySeconds": {
              "default": 5,
              "title": "initialDelaySeconds",
              "type": "integer"
            },
            "periodSeconds": {
              "default": 10,
              "title": "periodSeconds",
              "type": "integer"
            }
          },
          "required": [],
          "title": "readinessProbe",
          "type": "object"
        },
        "replicaCount": {
          "default": 1,
          "title": "replicaCount",
          "type": "integer"
        },
        "resources": {
          "description": "No limits by default (see the controller resources note). The webhook links\nthe same OpenTelemetry/OTLP stack but does no kopia work, so it stays light;\nonly the requests are set to reserve a fair share.",
          "properties": {
            "requests": {
              "properties": {
                "cpu": {
                  "default": "25m",
                  "title": "cpu",
                  "type": "string"
                },
                "memory": {
                  "default": "64Mi",
                  "title": "memory",
                  "type": "string"
                }
              },
              "required": [],
              "title": "requests",
              "type": "object"
            }
          },
          "required": [],
          "title": "resources",
          "type": "object"
        },
        "securityContext": {
          "description": "Container security context for the webhook.",
          "properties": {
            "allowPrivilegeEscalation": {
              "default": false,
              "title": "allowPrivilegeEscalation",
              "type": "boolean"
            },
            "capabilities": {
              "properties": {
                "drop": {
                  "items": {
                    "anyOf": [
                      {
                        "type": "string"
                      }
                    ],
                    "required": []
                  },
                  "title": "drop",
                  "type": "array"
                }
              },
              "required": [],
              "title": "capabilities",
              "type": "object"
            },
            "readOnlyRootFilesystem": {
              "default": true,
              "title": "readOnlyRootFilesystem",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "securityContext",
          "type": "object"
        },
        "serviceMonitor": {
          "description": "The webhook serves /metrics on its TLS port (kopiur_webhook_admission_total).\nThis ServiceMonitor stays on the webhook (not under monitoring:) because it\nis an HTTPS scrape of the webhook's own port — a different scrape config from\nthe controller's plain-HTTP one.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a ServiceMonitor scraping the webhook's /metrics over HTTPS.",
              "title": "enabled",
              "type": "boolean"
            },
            "insecureSkipVerify": {
              "default": true,
              "description": "The webhook serves a self-signed cert, so skip verification by default.",
              "title": "insecureSkipVerify",
              "type": "boolean"
            },
            "interval": {
              "default": "30s",
              "title": "interval",
              "type": "string"
            },
            "labels": {
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "scrapeTimeout": {
              "default": "10s",
              "title": "scrapeTimeout",
              "type": "string"
            }
          },
          "required": [],
          "title": "serviceMonitor",
          "type": "object"
        },
        "timeoutSeconds": {
          "default": 10,
          "description": "timeoutSeconds for admission requests (1..30).",
          "title": "timeoutSeconds",
          "type": "integer"
        },
        "tls": {
          "description": "TLS for the webhook server. The webhook ALWAYS serves TLS (k8s requires\nHTTPS for admission). How the serving cert is provisioned is chosen by\ntls.mode below.",
          "properties": {
            "mode": {
              "default": "self",
              "description": "How the webhook serving certificate is provisioned and trusted. One of:\n  self         — the operator mints its own CA + serving cert, writes the\n                 Secret, and injects caBundle into the webhook\n                 configurations itself. No cert-manager, no manual steps,\n                 and the leaf is auto-rotated before expiry. (default)\n  cert-manager — cert-manager issues the serving cert and its ca-injector\n                 populates caBundle (requires cert-manager installed;\n                 configure certManager.issuerRef below).\n  manual       — you pre-create the tls.secretName Secret (kubernetes.io/tls)\n                 and set webhook.caBundle (base64 PEM) yourself.",
              "title": "mode",
              "type": "string"
            },
            "secretName": {
              "default": "kopiur-webhook-tls",
              "description": "Name of the Secret holding tls.crt / tls.key (and, in self mode, ca.crt).\nIn self mode the operator creates and owns it; in cert-manager mode\ncert-manager writes it; in manual mode YOU create it before install.",
              "title": "secretName",
              "type": "string"
            }
          },
          "required": [],
          "title": "tls",
          "type": "object"
        },
        "tolerations": {
          "items": {
            "required": []
          },
          "title": "tolerations",
          "type": "array"
        },
        "topologySpreadConstraints": {
          "description": "Spread webhook replicas across nodes/zones (pairs with podDisruptionBudget).",
          "items": {
            "required": []
          },
          "title": "topologySpreadConstraints",
          "type": "array"
        }
      },
      "required": [],
      "title": "webhook",
      "type": "object"
    },
    "workerThreads": {
      "default": 2,
      "description": "Tokio worker threads for the controller runtime. The controller is I/O-bound,\nso a small pool is ample; the runtime default sizes to the host core count\n(ignoring the cgroup CPU quota), over-allocating worker threads — each a stack\nplus a malloc arena — on large nodes, inflating RSS for no throughput gain.\nRaise only for a reconcile-heavy deployment.",
      "title": "workerThreads",
      "type": "integer"
    }
  },
  "required": [],
  "type": "object"
}
